{
    "config": {
        "flow_title": "{model} {id}",
        "step": {
            "confirm": {
                "title": "Add {model} {id}",
                "description": "Discovered device \"{model}\" with id {id}. Sensors: {sensors}. Add to Home Assistant?"
            }
        }
    }
}
